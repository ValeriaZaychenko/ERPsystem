
$(document).ready(function () {

    $("#datepicker").datepicker(
        {
            viewMode: 'years',
            format: 'yyyy',
            minViewMode: "years",
            maxViewMode: "years"
        });

});
