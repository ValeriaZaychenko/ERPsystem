
$(document).ready(function () {

    $("#datepicker").datepicker(
        {
            viewMode: 'months',
            format: 'yyyy-mm',
            minViewMode: "months",
            maxViewMode: "months"
        });
});
